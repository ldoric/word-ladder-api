# PyTorch: install CPU wheels in Dockerfile first (smaller image). Do not add torch here
# to avoid a duplicate / CUDA install when building the Space image.
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0
huggingface-hub>=0.20.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
